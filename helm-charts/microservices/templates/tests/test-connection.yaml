apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservices.fullname" . }}-test-connection"
  labels:
{{ include "microservices.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "microservices.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
